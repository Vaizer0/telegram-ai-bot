# Python 3.10+ required.
# httpx is already a transitive dependency of python-telegram-bot; we list it
# explicitly because bot.py imports it directly.
python-telegram-bot>=21.0,<23
httpx>=0.27
python-dotenv>=1.0
